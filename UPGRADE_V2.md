# 🚀 VideoRUBY v2.0 - Новые возможности

## Что нового?

Мы добавили мощные новые функции для профессионального монтажа.

## 🤖 AI-анализ дублей (ai_analyzer.py)

**Автоматическое определение good/bad takes через искусственный интеллект**

### Возможности

✅ **Умное определение bad takes:**
- Повторения и переделывания фразы
- Филлеры ("эм", "аа", "это самое", "как бы")
- Оговорки и незавершенные мысли
- Обрывки фраз

✅ **Сопоставление со сценарием:**
- LLM анализирует соответствие тексту
- Находит "последние попытки" (финальные версии)
- Определяет ненужные дубли

✅ **Три режима работы:**
1. **Эвристический** (без API) - бесплатно
2. **OpenAI API** - максимальная точность
3. **Локальная LLM** (Ollama) - приватность

### Использование

```python
from ai_analyzer import AIAnalyzer

# Создаем анализатор
analyzer = AIAnalyzer()

# Анализируем транскрипцию
transcription = [
    {'start': 0.0, 'end': 3.0, 'text': 'Привет всем!'},
    {'start': 3.5, 'end': 6.0, 'text': 'эм, это, как бы, привет всем'},
    {'start': 6.5, 'end': 9.0, 'text': 'Привет всем! Сегодня поговорим о...'},
]

takes = analyzer.analyze_transcription(transcription)

# Получаем рекомендации
recommendations = analyzer.generate_recommendations(takes)

print(f"Bad takes: {recommendations['statistics']['bad_takes']}")
print(f"Экономия времени: {recommendations['statistics']['time_saved']:.1f}s")
```

### С OpenAI API (продвинутый режим)

```bash
# Установите ключ API
export OPENAI_API_KEY="your-api-key"

# Анализ будет использовать GPT-4o-mini
```

```python
analyzer = AIAnalyzer(api_key="your-key")
takes = analyzer.analyze_transcription(transcription, script="ваш_сценарий.txt")
```

### С локальной LLM (приватность)

```bash
# Установите Ollama
brew install ollama

# Скачайте модель
ollama pull llama3

# Запустите сервер
ollama serve
```

```python
analyzer = AIAnalyzer(use_local_llm=True)
takes = analyzer.analyze_transcription(transcription)
```

## 🎬 Multicam синхронизация (multicam_sync.py)

**Автоматическая синхронизация нескольких камер**

### Возможности

✅ **Три метода синхронизации:**
1. **Waveform** - По аудио-форме волны (самый точный)
2. **Timestamps** - По временным меткам файлов
3. **Clap** - По звуку хлопушки

✅ **Автоматический выбор референса**
✅ **Расчет уверенности синхронизации**
✅ **Экспорт для Final Cut Pro**

### Использование

```bash
# Синхронизация 3 камер
python multicam_sync.py камера1.mp4 камера2.mp4 камера3.mp4
```

**Результат:**
```
📊 РЕЗУЛЬТАТЫ СИНХРОНИЗАЦИИ
════════════════════════════════════════

📹 камера1.mp4
   Смещение: Референс
   Уверенность: 100.0%
   Метод: reference

🎥 камера2.mp4
   Смещение: +2.345s
   Уверенность: 94.2%
   Метод: waveform

🎥 камера3.mp4
   Смещение: +2.312s
   Уверенность: 95.8%
   Метод: waveform
```

### Интеграция в workflow

```python
from multicam_sync import MulticamSync

# Создаем синхронизатор
syncer = MulticamSync(reference_video='main_camera.mp4')

# Синхронизируем все камеры
videos = ['main_camera.mp4', 'camera2.mp4', 'camera3.mp4']
results = syncer.sync_videos(videos, method='waveform')

# Экспортируем для FCP
syncer.export_fcpxml_multicam(results, 'multicam_project.fcpxml')
```

## 📦 Полный workflow v2.0

### Сценарий 1: Подкаст с 2 камерами

```bash
# 1. Синхронизируем камеры
python multicam_sync.py host_cam.mp4 guest_cam.mp4

# 2. Обрабатываем основную камеру
python video_editor_app.py
# (Выбираем host_cam.mp4, обрабатываем)

# 3. Анализируем дубли через AI
python -c "
from ai_analyzer import AIAnalyzer
from transcription import Transcriber
import json

# Транскрибируем
transcriber = Transcriber()
subtitle_path = transcriber.transcribe('host_cam_edited.mp4', 'json')

# Читаем транскрипцию
with open(subtitle_path) as f:
    data = json.load(f)

# Анализируем
analyzer = AIAnalyzer()
takes = analyzer.analyze_transcription(data['segments'])

# Сохраняем анализ
analyzer.export_analysis(takes, 'analysis.json')
"

# 4. Импортируем в Final Cut Pro
# - Multicam clip из синхронизированных видео
# - Субтитры
# - Используем analysis.json для ручной проверки bad takes
```

### Сценарий 2: Solo видео с максимальным AI

```bash
# Запускаем с AI анализом
python video_editor_app.py

# После обработки:
# 1. Видео без пауз ✅
# 2. Субтитры ✅
# 3. AI определил bad takes ✅
# 4. FCPXML проект готов ✅
```

## 🆕 Новые зависимости

```bash
# Для AI-анализа (опционально)
pip install openai  # Для OpenAI API
# ИЛИ
brew install ollama  # Для локальной LLM

# Для multicam (уже установлено)
pip install numpy  # Для обработки аудио
```

## 📊 Сравнение версий

| Функция | v1.0 | v2.0 |
|---------|------|------|
| Удаление пауз | ✅ | ✅ |
| Субтитры | ✅ | ✅ |
| FCPXML | ✅ | ✅ |
| AI анализ bad takes | ❌ | ✅ |
| Multicam синхронизация | ❌ | ✅ |
| LLM интеграция | ❌ | ✅ |
| Сопоставление сценария | ❌ | ✅ |
| Детекция филлеров | ❌ | ✅ |

## 🎯 Рекомендации по использованию

### Для YouTube/подкастов
1. Используйте **AI-анализ** для определения bad takes
2. Включите **удаление пауз** (как в v1.0)
3. Добавьте **субтитры** для доступности

### Для интервью/многокамерных съемок
1. **Multicam sync** для синхронизации
2. Обработайте основную камеру через v1.0
3. AI-анализ для финальной проверки

### Для обучающих видео
1. **Сопоставление со сценарием** через AI
2. Автоматическое определение отклонений
3. Удаление повторений

## 🔮 Планы на v3.0

- [ ] Полная автоматизация multicam монтажа
- [ ] Детектирование эмоций и качества речи
- [ ] Автоматический выбор лучших ракурсов
- [ ] Интеграция с Adobe Premiere и DaVinci Resolve
- [ ] Веб-интерфейс вместо desktop app
- [ ] Облачная обработка (опционально)

## 💡 Советы по использованию AI

### Выбор режима анализа

**Эвристический (бесплатно):**
- Подходит для: простых видео, типичные филлеры
- Точность: ~70-80%
- Скорость: мгновенно

**OpenAI API (~$0.50 за час видео):**
- Подходит для: профессиональный контент
- Точность: ~95%
- Скорость: 1-2 минуты на час видео

**Локальная LLM (бесплатно + приватность):**
- Подходит для: конфиденциальные материалы
- Точность: ~85-90% (зависит от модели)
- Скорость: зависит от железа

### Оптимизация затрат

1. Используйте **эвристический** анализ сначала
2. Применяйте **OpenAI** только для важных частей
3. Для регулярной работы настройте **локальную LLM**

## 📚 Дополнительные ресурсы

- [OpenAI API Docs](https://platform.openai.com/docs) - Для AI-анализа
- [Ollama](https://ollama.ai) - Локальные LLM модели

---

**Версия**: 2.0
**Дата**: Январь 2026
**License**: MIT

Наслаждайтесь новыми возможностями! 🎉
