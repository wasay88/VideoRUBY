# 🚀 Быстрый старт - VideoRUBY

## За 3 шага к работающей системе

### Шаг 1: Установка (5-10 минут)

Откройте **Terminal** и перейдите в папку с файлами:

```bash
cd /путь/к/VideoRUBY
```

Запустите установку:

```bash
chmod +x install.sh
./install.sh
```

### Шаг 2: Запуск

```bash
python3 video_editor_app.py
```

Или создайте .app для запуска двойным кликом:

```bash
chmod +x create_app.sh
./create_app.sh
```

Теперь запускайте **VideoRUBY.app** двойным кликом!

### Шаг 3: Обработка видео

1. В программе нажмите **"Обзор..."**
2. Выберите ваше видео (MP4, MOV)
3. Нажмите **"ОБРАБОТАТЬ ВИДЕО"**
4. Дождитесь завершения (2-5 минут на 10-минутное видео)

## Результат

В папке с видео появятся:
- `ваше_видео_edited.mp4` - Видео без пауз
- `ваше_видео_edited.srt` - Субтитры
- `ваше_видео_edited.fcpxml` - Проект для Final Cut Pro

## Импорт в Final Cut Pro

1. `File → Import → Files...`
2. Выберите `.fcpxml` файл
3. Проект откроется с видео
4. `File → Import → Captions...`
5. Выберите `.srt` файл

## Настройки (рекомендуемые)

Оставьте значения по умолчанию:
- Порог тишины: **-35 dB**
- Мин. пауза: **0.5 сек**
- Модель: **base** (оптимально)

## Решение проблем

### "ffmpeg не найден"
```bash
brew install ffmpeg
```

### "whisper не найден"
```bash
pip3 install openai-whisper --break-system-packages
```

### macOS блокирует .app
1. `Системные настройки → Конфиденциальность`
2. Нажмите "Открыть в любом случае"

---

📖 **Полная документация**: README.md
