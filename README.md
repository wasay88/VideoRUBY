# 🎬 VideoRUBY

**Локальная система автоматической обработки видео для Final Cut Pro**

Локальная система обработки, которая работает на вашем Mac без отправки данных в облако.

## ✨ Возможности

- 🗣️ **Автоматическое распознавание русской речи** через OpenAI Whisper
- ✂️ **Умное удаление пауз** из видео
- 📝 **Генерация субтитров** в форматах SRT и FCPXML
- 🎞️ **Прямая интеграция с Final Cut Pro** (версия 10.1.2+)
- 🔒 **100% локальная обработка** - ваши видео не покидают компьютер
- 🆓 **Бесплатно и open-source**

## 📋 Требования

- macOS (10.15 или новее)
- Final Cut Pro 10.1.2 или новее
- Python 3.8+
- Homebrew
- ~5 GB свободного места (для моделей Whisper)

## 🚀 Установка

### Шаг 1: Скачайте файлы

Скачайте все файлы проекта в одну папку:
- `video_editor_app.py` (главное приложение)
- `video_processor.py` (обработка видео)
- `transcription.py` (транскрипция)
- `fcpxml_generator.py` (генератор FCPXML)
- `install.sh` (скрипт установки)
- `create_app.sh` (создание .app)
- `README.md` (эта инструкция)

### Шаг 2: Установите зависимости

Откройте **Terminal** (Терминал) и перейдите в папку с файлами:

```bash
cd /путь/к/папке/с/файлами
```

Запустите установку:

```bash
chmod +x install.sh
./install.sh
```

Скрипт автоматически установит:
- ffmpeg (обработка видео)
- OpenAI Whisper (распознавание речи)
- Необходимые Python библиотеки

⏱️ **Время установки:** ~5-10 минут (зависит от скорости интернета)

### Шаг 3: Создайте приложение (опционально)

Для удобства можно создать .app приложение:

```bash
chmod +x create_app.sh
./create_app.sh
```

Появится приложение **VideoRUBY.app** которое можно запускать двойным кликом.

## 🎯 Как использовать

### Вариант 1: Через GUI приложение (рекомендуется)

1. **Запустите программу:**
   - Двойной клик на **VideoRUBY.app**
   - Или в Terminal: `python3 video_editor_app.py`

2. **Выберите видео:**
   - Нажмите "Обзор..." и выберите MP4/MOV файл

3. **Настройте параметры:**
   - **Порог тишины (-35 dB)**: Чем ниже значение, тем больше пауз будет найдено
   - **Мин. длительность паузы (0.5 сек)**: Минимальная длина паузы для удаления
   - **Модель Whisper (base)**: Качество транскрипции
     - `tiny` - быстро, но менее точно
     - `base` - оптимально (рекомендуется)
     - `small` - лучше качество, медленнее
     - `medium/large` - максимальное качество, очень медленно

4. **Нажмите "ОБРАБОТАТЬ ВИДЕО"**

5. **Дождитесь завершения:**
   - Программа проанализирует аудио
   - Удалит паузы
   - Создаст субтитры
   - Сгенерирует FCPXML проект

⏱️ **Время обработки:**
- 10-минутное видео ≈ 2-5 минут (модель base)
- При первом запуске Whisper скачает модель (~150 MB)

### Вариант 2: Через командную строку

```bash
# Только удаление пауз
python3 video_processor.py мое_видео.mp4

# Только транскрипция
python3 transcription.py мое_видео.mp4

# Полная обработка через GUI
python3 video_editor_app.py
```

## 📦 Результаты обработки

После обработки в той же папке что и исходное видео появятся:

- `ваше_видео_edited.mp4` - Видео без пауз
- `ваше_видео_edited.srt` - Субтитры (универсальный формат)
- `ваше_видео_edited.fcpxml` - Проект для Final Cut Pro

## 🎬 Импорт в Final Cut Pro

### Способ 1: Импорт FCPXML (рекомендуется)

1. Откройте **Final Cut Pro**
2. `File → Import → Files...`
3. Выберите файл `ваше_видео_edited.fcpxml`
4. Откроется готовый проект с видео на timeline
5. Импортируйте субтитры: `File → Import → Captions...`
6. Выберите файл `ваше_видео_edited.srt`

### Способ 2: Ручной импорт

1. Импортируйте отредактированное видео
2. Перетащите на timeline
3. `File → Import → Captions...`
4. Выберите SRT файл

## ⚙️ Настройка параметров

### Порог тишины (Silence Threshold)

Определяет что считается "тишиной":

- **-30 dB** - Удалит только очень тихие места
- **-35 dB** - Оптимально для большинства записей
- **-40 dB** - Агрессивное удаление (может вырезать тихую речь!)

💡 **Рекомендация**: Начните с -35 dB, если результат не устраивает - корректируйте.

### Минимальная длительность паузы

- **0.3 сек** - Удалит даже короткие паузы (может быть слишком агрессивно)
- **0.5 сек** - Оптимально
- **1.0+ сек** - Удалит только длинные паузы

### Модель Whisper

| Модель | Размер | Скорость | Точность | Рекомендация |
|--------|--------|----------|----------|--------------|
| tiny   | 75 MB  | Очень быстро | Низкая | Для тестов |
| base   | 150 MB | Быстро | Хорошая | ⭐ Рекомендуется |
| small  | 500 MB | Средне | Отличная | Для важных видео |
| medium | 1.5 GB | Медленно | Превосходная | Профессионалам |
| large  | 3 GB   | Очень медленно | Лучшая | Максимум качества |

### Качество видео (CRF / Битрейт)

- **CRF**: качество кодирования. Меньше = лучше, но больше размер файла и дольше обработка.
  - Рекомендуемое значение: **16** (по умолчанию — максимум качества).
- **Битрейт**: если задан (например, `20M`), то CRF игнорируется и используется фиксированный битрейт.

## 🔧 Решение проблем

### "ffmpeg не установлен"

```bash
brew install ffmpeg
```

### "Whisper не установлен"

```bash
pip3 install openai-whisper --break-system-packages
```

### "Permission denied" при запуске скриптов

```bash
chmod +x install.sh create_app.sh
```

### macOS блокирует запуск .app

1. `Системные настройки → Конфиденциальность и безопасность`
2. Внизу будет сообщение о заблокированном приложении
3. Нажмите "Открыть в любом случае"

### Программа не находит Whisper

Если после установки `pip3 install openai-whisper` программа не видит Whisper:

```bash
# Проверьте где установлен whisper
which whisper

# Если ничего не найдено, убедитесь что путь к Python bin в PATH:
echo 'export PATH="$HOME/Library/Python/3.*/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

### Ошибка "Model not found"

При первом запуске Whisper автоматически скачает модель. Убедитесь что есть интернет.

## 🛣️ Планы развития

- [ ] Детектирование и удаление "bad takes" через AI
- [ ] Автоматическое определение оптимальных параметров
- [ ] Поддержка других NLE (Premiere Pro, DaVinci Resolve)
- [ ] Автоматическая обработка папок
- [ ] Веб-интерфейс
- [ ] Распознавание других языков

## 💡 Советы по использованию

1. **Для длинных видео (>30 мин)** используйте модель `base` или `small`
2. **Для коротких роликов** можно использовать `medium` для лучшего качества
3. **Экспериментируйте с порогом тишины** - оптимальное значение зависит от вашего микрофона
4. **Проверяйте результат** перед удалением оригинала
5. **Делайте бэкапы** важных видео перед обработкой

## 📚 Технические детали

### Как это работает

1. **Анализ аудио**: ffmpeg silencedetect находит паузы в аудиодорожке
2. **Сегментация**: Видео разбивается на сегменты (речь/пауза)
3. **Удаление пауз**: Речевые сегменты склеиваются через ffmpeg concat
4. **Транскрипция**: OpenAI Whisper распознает речь (модель tiny/base/small/medium/large)
5. **Генерация субтитров**: Создаются SRT и FCPXML файлы
6. **FCPXML**: XML файл с timeline для Final Cut Pro

### Используемые технологии

- **ffmpeg** - Обработка видео/аудио
- **OpenAI Whisper** - Распознавание речи (state-of-the-art)
- **Python 3** - Основной язык
- **tkinter** - GUI интерфейс
- **FCPXML** - Формат Final Cut Pro

## 🤝 Вклад в проект

Проект открыт для улучшений! Если вы хотите добавить функции:

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Сделайте pull request

## 📄 Лицензия

MIT License - свободное использование и модификация.

## 🙏 Благодарности

- [OpenAI Whisper](https://github.com/openai/whisper) - За отличную модель распознавания речи
- Сообщество FFmpeg

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Убедитесь что все зависимости установлены: `./install.sh`
3. Проверьте версии: `ffmpeg -version`, `whisper --help`

---

**Made with ❤️ for Final Cut Pro editors**

*Локальная обработка видео без облака - ваши данные остаются у вас!*
